# Replit run notes

- Mission: make the app work with the minimum necessary code.
- Start the web companion with `npm run dev`.
- The Express/Vite server uses `PORT` when provided and defaults to `5000`.
- The Android project remains under `android-shell/` and is built separately with Gradle.
- Keep browser-only subtitle fallbacks and demo fixtures separate from the Android native bridge.